{
  "name": "@appneural/spatial-studio",
  "version": "0.1.0",
  "private": true,
  "description": "Spatial Studio — served at spatial.studio.appneural.com",
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy --env production"
  },
  "devDependencies": {
    "wrangler": "^4.119.0"
  },
  "engines": {
    "node": ">=20"
  }
}
